<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.allaber.pass" >

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/string_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/SplashScreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".scanner.ScannerActivity"
            android:label="@string/string_qr_scanner"
            android:exported="false" />
        <activity
            android:name=".start.StartActivity"
            android:label="@string/string_app_name"
            android:exported="false" />
        <activity
            android:name=".home.navigator.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".slider.navigator.SliderActivity"
            android:exported="false" />
    </application>

</manifest>